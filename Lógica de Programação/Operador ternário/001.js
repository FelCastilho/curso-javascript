const pontoUser = 1000;
const nivelUser = pontoUser >= 1000 ? 'Usuário VIP' : 'Usuário Normal';

console.log(nivelUser);

//(condição) ? valor para verdadeiro : valor para falso

/*
if(pontoUser >= 1000){
    console.log('Usuário Vip');
}else{
    console.log('Usuário normal');
}
*/
