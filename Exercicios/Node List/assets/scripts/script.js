const paragrafos = document.querySelector('.paragrafos');
//Selecionando todos os paragrafos dentro da div
const ps = paragrafos.querySelectorAll('p'); //Retorna um Array

//Pegando todos os elementos computados no body

for (let p in ps){
   
}
ps.style.background = 'red';
